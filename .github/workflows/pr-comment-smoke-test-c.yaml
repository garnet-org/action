name: PR Comment Smoke Test C

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  smoke-c:
    name: Smoke C
    uses: ./.github/workflows/pr-comment-smoke-test-reusable.yaml
    with:
      job_name: Smoke C
    secrets: inherit
