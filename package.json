{
  "name": "garnetai-action",
  "version": "1.0.0",
  "description": "GarnetAI GitHub Action",
  "private": true,
  "scripts": {
    "build": "ncc build src/main.js -o dist/main --license licenses.txt && ncc build src/post.js -o dist/post --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "tar": "^7.5.9",
    "upload-artifact": "github:actions/upload-artifact#v4"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
