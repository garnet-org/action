#!/bin/sh
set -eu

if ! git diff --cached --name-only --diff-filter=ACMR | grep -Eq '^(src/|package\.json$|package-lock\.json$|action\.yaml$|jsconfig\.json$)'; then
  exit 0
fi

echo "pre-commit: running npm run build"
npm run build
git add dist/